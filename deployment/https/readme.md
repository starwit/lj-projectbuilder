This is configured for running on server for domain lj.starwit.de.

* configure domain
* login to server via ssh
* **!!! set environment variables CLIENTID and CLIENTSECRET**
* execute `init-letsencrypt.sh`
* execute `docker-compose up -d`