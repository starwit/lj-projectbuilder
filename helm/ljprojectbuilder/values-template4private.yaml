# Private values for helm install.

mariadb:
  auth:
    rootPassword: root #change
    password: ljprojectbuilder #change

github:
  registry:
    username: change
    pat: change

oauth:
  clientid: change
  clientsecret: change
