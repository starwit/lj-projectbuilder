import {makeStyles} from "@mui/styles";

const EntityDiagramButtonStyles = makeStyles(theme => ({
    fabWrapper: {
        position: "absolute",
        bottom: "5%",
        right: "2%"
    }
}));

export default EntityDiagramButtonStyles;
