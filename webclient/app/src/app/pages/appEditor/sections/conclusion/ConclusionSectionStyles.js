import {makeStyles} from "@mui/styles";

const ConclusionSectionStyles = makeStyles(theme => ({
    downloadGrid: {
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        width: "100%"
    },
    list: {
        width: "100%"
    },
    root: {
        paddingTop: "2rem"
    }
}));
export default ConclusionSectionStyles;