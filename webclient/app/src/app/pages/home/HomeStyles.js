import {makeStyles} from "@mui/styles";

const HomeStyles = makeStyles(theme => ({
    addFab: {
        position: "absolute",
        bottom: "5%",
        right: "2%"
    },
}));
export default HomeStyles;