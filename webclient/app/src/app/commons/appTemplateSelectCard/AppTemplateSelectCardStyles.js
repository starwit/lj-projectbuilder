import {makeStyles} from "@mui/styles";

const AppTemplateSelectCardStyles = makeStyles(theme => ({
    actionsWrapper: {
        justifyContent: "flex-end"
    },
    cardMedia: {
        backgroundColor: "#000"
    }
}))
export default AppTemplateSelectCardStyles;