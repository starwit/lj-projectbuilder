import {makeStyles} from "@mui/styles";

const GitDownloadButtonStyles = makeStyles(theme => ({
    actionsWrapper: {
        justifyContent: "flex-end"
    },
    cardMedia: {
        backgroundColor: "#000"
    }
}))
export default GitDownloadButtonStyles;